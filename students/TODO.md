# TODO: Add Image Display in Output

## Steps to Complete
- [ ] Modify `stuapp/views.py` to save uploaded images permanently in the media directory and include the image URL in the JSON response.
- [ ] Update `templates/index.html` to display the uploaded image using the URL from the server response instead of client-side FileReader.
- [ ] Test the image upload and display functionality to ensure it works correctly.
